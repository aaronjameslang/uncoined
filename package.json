{
  "name": "uncoined",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "jsdoc": "rm -rf docs && jsdoc dist --recurse --destination docs --configure jsdoc.json",
    "lint": "tslint --fix --project .",
    "posttest": "npm run lint && npm run jsdoc",
    "pretest": "rm -rf dist && tsc",
    "test": "mocha --require jsdoctest dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "big-integer": "^1.6.26",
    "jsdoc": "^3.5.5",
    "jsdoctest": "^1.7.1",
    "mocha": "^5.0.2",
    "standard": "^10.0.3",
    "typescript": "^2.8.3"
  },
  "standard": {
    "ignore": [
      "docs"
    ]
  },
  "dependencies": {
    "tslint": "^5.10.0",
    "tslint-config-standard": "^7.0.0"
  }
}
